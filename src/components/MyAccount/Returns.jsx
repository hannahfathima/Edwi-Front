import React from 'react';
import './Returns.scss';

const Returns = () => {
    return (
        <section className="dashboard-section">
            <h2 className="section-heading">RETURNS</h2>
            <div className="placeholder-content">
                <p>No returns found yet.</p>
            </div>
        </section>
    );
};

export default Returns;
